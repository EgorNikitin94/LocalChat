{% include 'header' %}

import Foundation
import Combine

class {{ prefix }}{{ module_info.name }}Model: ObservableObject, {{ prefix }}{{ module_info.name }}ModelStateProtocol {
  let routerSubject = {{ prefix }}{{ module_info.name }}Router.Subjects()
}

// MARK: - Actions
extension {{ prefix }}{{ module_info.name }}Model: {{ prefix }}{{ module_info.name }}ModelActionsProtocol {
  
}

// MARK: - Route
extension {{ prefix }}{{ module_info.name }}Model: {{ prefix }}{{ module_info.name }}ModelRouterProtocol {
  
}